html
	head
		title #{judul}
	body
		form(action="/save", method="POST")
			div
				label(for="say") Say :
				input(name="say", placeholder="silahkan isi")
			br
			div
				label(for="to") To:
				input(name="to", placeholder="ex : Andika")
			br
			button(type="submit") Send